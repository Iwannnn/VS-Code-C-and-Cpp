#include <stdio.h>
int main(int argv,char **argc){
    int n, m1, m2,i,j;
    scanf("%d%d%d", &n,&m1,&m2);
    int a[n],t[n],*p=a,*q=t;
    for (i = 0; i < n;i++)
        scanf("%d", p + i);
    for (i = 0; i < n;i++){
        
    }
        return 0;
}