#include <stdio.h>
int main(int argc,char *argv[]){
    char cube;
    int m, n, i, j, k;
    scanf("%d%d", &m, &n);
    int f[m][n];
    for (i = 0; i < m; i++)
        for (j = 0; j < n;j++)
            scanf("%d", &f[i][j]);
    printf("  %s", );

    return 0;
}