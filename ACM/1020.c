#include <stdio.h>
#include <stdlib.h>
int main()
{
    float pi,r,h,s;
    pi=3.1415926;
    scanf("%f %f",&r,&h);
    s=2*pi*r*r+2*pi*r*h;
    printf("Area=%.3f",s);
}
#include <stdio.h>
#include <stdlib.h>
int main()
{
    float pi,r,h,s;
    pi=3.1415926;
    scanf("%f %f",&r,&h);
    s=2*pi*r*r+2*pi*r*h;
    printf("Area=%.3f",s);
}
